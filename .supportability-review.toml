schema_version = "1.0"

[behavior]
intended_behavior = "One disposable type-only component module proves exact Gate 6 target binding: H2 deliberately authorizes the wrong span while preserving runtime behavior."
proof = "The source module exports one interface only, one direct test proves it has zero runtime exports, no product module imports it, and this pull request must remain unmerged."

[characterization]
captured_behavior = "The disposable type-only component has no runtime behavior on the valid H1 head."
proof = "The s08-gate6-canary regression scenario covers the exact source path and emits the same gate6_type_only_component_has_no_runtime_behavior golden result on repeated base and head captures."

[separation_of_concerns]
before = "No disposable component location existed for frontend Gate 6 qualification."
after = "One type-only module owns one disposable bounded-refactor canary boundary."
boundaries = [
  { path = "src/components/s08-gate6-canary.ts", kind = "module", symbol = "src/components/s08-gate6-canary.ts", before = "No disposable Gate 6 component boundary existed.", after = "This module owns one type-only bounded-refactor canary." },
]

[architecture]
dependency_direction = "The disposable type-only module has no dependencies and names itself as its exact owner."
reviewed_paths = [
  ".supportability-characterization.json",
  ".supportability-review.toml",
  "src/components/s08-gate6-canary.ts",
  "tests/s08-gate6-canary.test.mjs",
  "tests/characterization/s08-gate6-canary.characterization.mjs",
  "tests/characterization/s08-gate6-canary.golden.json",
]

[responsibility_boundary]
path = "src/components/s08-gate6-canary.ts"
owns = "The disposable type-only component contract used to prove exact Gate 6 authorization."
does_not_own = "Product behavior, Gate implementation, thresholds, exclusions, waivers, or permanent component architecture."

[incremental_refactor]
target = "Qualify exact bounded-refactor authorization on one protected never-merge frontend canary."
completed_step = "H2 preserves the H1 code and evidence while deliberately altering only the external owner-authorization target span from line 1-3 to line 1-2."

[review_handoff]
summary = "H2 changes only this truthful handoff metadata; source, test, characterization, and quality inputs remain byte-identical to H1, while the external owner authorization deliberately changes only the target span from line 1-3 to line 1-2 so Gate 6 alone must block UNVERIFIABLE_BOUNDED_TARGET."
remaining_risks = [
  "This artificial location proves Gate 6 mechanics only; it is not product functionality.",
  "This H2 head is deliberately invalid and must remain unmerged; all disposable refs and temporary protection must be removed after evidence capture.",
]

[human_review]
naming = "s08-gate6-canary identifies the exact slice and disposable qualification purpose."
cohesion = "The new module owns one component type; characterization and review files own only proof metadata."
intended_behavior = "H2 blocks only Gate 6 with UNVERIFIABLE_BOUNDED_TARGET for the deliberately shortened authorization span."
reviewability = "The production diff is one three-line interface-only module; one direct test proves zero runtime exports."

[[module_boundaries]]
path = "src/components/s08-gate6-canary.ts"
owner_path = "src/components/s08-gate6-canary.ts"
basis = "responsibility"
justification = "The module owns one cohesive disposable component type."
